import 'package:flutter/material.dart';

var drawer = Drawer(
    child: Column(
        mainAxisAlignment: MainAxisAlignment.start,
        mainAxisSize: MainAxisSize.min,
        children: const <Widget>[Text('item1'), Text('item1'), Text('item1')]));
